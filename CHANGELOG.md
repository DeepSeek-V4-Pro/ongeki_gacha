# 更新日志

## 1.3.0 - 2026-09-21

> 发布日期：2026-09-21。

本次更新在原有“抽卡模拟器”之上加入主角色好感养成，统一了各指令的引导与图片输出，
并把素材与发布包分离。抽卡权重、抽卡价格与 1.2 的卡池模型保持不变。

### 新增

- **主角色好感养成（17 名主角色全部开放）**：用 `/伙伴 <角色姓名>` 选择伙伴后，
  `/陪伴` 每天可提升 300 好感，`/送礼 <角色姓名> 小|中|大 [数量]` 消耗礼物提升好感；
  `/好感 [角色姓名|列表]` 打开好感页或角色总览，显示等级、累计好感、基础资料与资料解锁。
- **基础 N 卡首获改为好感页懒获取**：不再建号时批量发 17 张卡；每名角色的第一张
  基础 N 卡在首次查看 `/好感 <角色姓名>` 时获得并发送揭示图，避免刷屏。之后
  10 张仍由好感节点发放至 11 星。
- **好感无封顶**：Lv1000（10 档）之后按最高档每级 15,600 点继续累计，心形最多显示 99/99
  （Lv9999），累计点数不封顶；陪伴与送礼不再因满档被截断。
- **好感奖励与装扮**：每名角色 32 项固定奖励（10 条档案语音、9 个称号、10 张基础 N 卡、
  2 个名牌、1 个装饰），达到等级自动领取，可用 `/好感奖励 [角色姓名]` 分页查看。
  `/装扮` 可查看、装备或卸下称号与装饰；新获得的称号与装饰会自动装备，同类型更高节点
  覆盖为最新一件。名牌只作收藏，不参与展示。
- **角色语音**：`/角色语音 <角色姓名> [序号]` 手动点播已解锁的档案语音；小礼物、中/大礼物、
  好感升级会在养成成功后自动回应一条（升级优先）。`/角色语音 分类` 查看自动回应与手动播放
  的区别，手动与自动共用可配置的冷却与每分钟限制；限流与请求占位写入数据库，重启不清零。
- **解花 / 超解花**：解花不要求卡牌满星，需角色好感 Lv100 并消耗 1 张解花券；
  超解花保留满星要求（N卡11星、其他5星），并需已解花、角色好感 Lv200，消耗 90 花之碎片。解花券暂定由高级挑战
  目标定数≥13.5、SSS/SSS+通关获得 1 张，获得后 15 天冷却。成功后发送阶段对照图。
- **高级挑战任务**：`/接任务 高级挑战` 每日 1 次，从至少有一张定数 ≥12.7 谱面的歌曲中
  随机并选取最低达标谱面，要求该谱面或以上 S 评级；奖励 S/SS/SSS 为 105/115/125 点。
- **终极任务按难度记录**：同一曲目若有多个达标难度，会作为不同任务分别记录；
  已完成难度不再重复抽取，其他达标难度仍可接取。
- **卡册按角色分页**：`/卡册` 显示总览（17 名主角色＋其他），`/卡册 <角色姓名> <页码>`
  单次只发一页，列出卡ID、稀有度与持有数量；非主角色统一归入“其他”，翻页由用户指定页码。
- **物品获得图**：签到活动、礼物购买、任务审核与终极任务发放养成物品时，额外发送获得提示图；
  礼物使用原作素材，花之碎片使用花瓣素材示意。
- **素材与发布包分离，配套文字兜底**：发布包只带代码、文档、工具和 JSON 索引，
  卡面、界面素材、字体、养成图片与语音都不进上传列表，由使用者按
  [素材指南](ASSETS.md) 在本地补齐。插件在完全没有素材时仍能加载并响应全部命令：
  长文本用自绘渐变背景渲染成分页图片，抽卡结果、揭示图、`/卡图`、好感页、奖励页、
  解花对照图与任务卡改为文字输出；没有可用的中日文字体时全部退化为纯文字。
- **新增兜底验证工具**：`python -m ongeki_gacha.tools.verify_text_fallback`
  会复制一份不含任何二进制的副本并实际跑一轮命令，用于确认发布包的兜底行为。
- `/点数` 现在同时显示花之碎片与解花券数量。

### 变更

- **好感曲线按原作校正**：原作每级需求为 `Point×Scale/100`，旧实现误用 `/1000`，
  使整条曲线只有原作的 1/10、礼物相对等级过高。现改为原作内部曲线：Lv100=165,000、
  Lv200=363,000、Lv1000=4,455,000，1000 级后每级 15,600；礼物沿用原作内部值
  300/1000/10000（萌娘百科显示值为 6/20/200），比例一致。旧好感点在迁移时 ×10，保留现有等级。
- **好感量表分档**：低档使用 `GaugeBase`，10 档及以上使用 `GaugeBase_10`；
  1000 级后大小两个心形都使用 `GaugeBase_Rebirth_10` 彩底，不再使用黄色满档素材。
- **卡牌揭示图**：SSR 抽到后额外发送一张原作风格揭示图；基础 N 卡的 1 星首获与好感节点升星、
  签到彩蛋卡改用同一揭示图。版式使用原作稀有度 Logo、属性图标和卡框，
  展示卡名、角色、限界突破星级与 MAX Lv，不展示技能和攻击力。
- **默认经济平衡 v7**：普通任务 3 次 × 40→35，新增高级挑战 1 次/天；
  日常任务点数上限 320→430（+34.4%，低于 35% 约束），终极 30000 点不变。
  抽卡价格 50/250/500、月卡 1000 点/30 天、囤点门槛与奖励、礼物价格与周额度保持不变。
- **签到活动周**：每月 1-7 日的签到除点数外还发放养成物品——第 1/3 天小礼物、第 5 天中礼物、
  第 7 天大礼物、第 2/4/6 天花之碎片（每日 5）；活动之外的签到只发点数。
  解花改用解花券后碎片只用于超解花，固定来源下调为活动周每日 5、任务审核每次 1 且每日上限 2，
  避免碎片单向累积。
- **任务奖励扩展**：普通/挑战/高级挑战审核成功会额外发放花之碎片（各 1，每日合计最多 2），
  挑战/高级挑战每日最多额外 1 份中礼物；终极任务仍只发点数。
- **配置项扩展**：好感规则、签到活动数值、任务碎片与中礼物来源、礼物购买额度、
  解花券ID/门槛/消耗、语音限流、卡池轮替起点、渲染缓存与短回复阈值均可配置；
  仍可用 `[growth] rules_overrides` 覆盖随包规则。
- **指令入口收敛**：每个功能只保留一个标准命令，移除 `/og*`、`/打卡`、`/图鉴`、`/池子`、
  `/角色`、`/播放语音`、`/语音分类` 等重复写法；角色总览改为 `/好感 列表`，
  语音分类改为 `/角色语音 分类`。
- **角色姓名默认显示原作写法**（如 `星咲 あかり`），常用中文写法继续可用；
  帮助、示例与奖励页统一使用原作名。
- **引导与文案**：错误提示会给出用法示例，操作回执会补充下一步命令，占位符与分隔符统一；
  `/卡池 下一期` 使用模拟器内的复刻档期，原公告日期单独标注为参考。

### 修复

#### 影响 1.2.x 已发布版本的问题

这三项在升级到 1.3.0 后生效，升级前会影响正在运行的实例。

- **抽卡结算不再拆成两步**：抽卡资格、扣点、库存、碎片、天井和消息去重改为同一事务提交。
  修复前，半价或周保底预扣后中途退出会丢失资格，平台重投同一条消息还可能再次扣点发卡；
  现在重复请求返回原回执，失败不会损失点数、半价次数或周保底。
- **卡池官方公告图可以正常下载**：下载函数写成了静态方法却引用了 `self.config`，
  触发 `NameError` 后被发送层吞掉，1.2.x 实际上从未成功发送过公告图。
- **抽卡数量写错时提示正确**：`/抽卡 3` 之类会被当成卡池编号，误报“指定卡池未启用”；
  现在明确提示数量只能是 1、5、11，并给出示例。

#### 1.3.0 制作过程中发现并修复的问题

下列问题只存在于 1.3 的开发过程，没有随任何已发布版本提供给用户，这里保留记录以便复查。

- **基础 N 卡少一张**：先送礼领取好感节点卡、之后再打开好感页时，会被误判为已发过首张，
  最终只有 10 张。现在首次查看会对比已领取的节点卡数量单独补齐，重复查看不会重复补发。
- **语音限流状态重启清零**：冷却、每分钟次数与请求占位原先只存在内存，
  且短 TTL 会清掉分钟计数、超过 60 秒的冷却被截断。现在改用可持久化的 Unix 时间，
  去重、冷却与分钟计数分别保留窗口并写入数据库。
- **终极任务缺少数据库层防重**：重复接取同谱面、或旧任务与完成记录重叠时可能重复发放 30000 点；
  现在接取与审核都在事务内校验“曲目＋难度”，并兼容旧的整曲锁。
- **待发送的揭示事件被提前清空**：原先读取整批事件就删除，图片发送失败或任务中断后无法恢复；
  现在确认单条发送成功后再删除，失败保留队列。
- **后续迁移不自动备份**：已有 growth-schema-v1 的用户在升级好感曲线时不会先备份；
  现在检查全部已知迁移，库内有用户且有待执行迁移时先生成一致性备份。
- **配置版本号漂移**：配置模型的 `config_version` 默认值仍是 1.2.1，
  与 `_manifest.json`、`__init__.py` 的 1.3.0 不一致，配置版本比较失效。
- **渲染缓存清理范围不全**：只清理 4 类旧文件名，1.3 新增的揭示图、物品图、卡册页、
  好感页与解花对照图会一直堆积在运行目录；现已覆盖全部一次性产物，
  卡池公告图与曲绘缓存仍按各自 TTL 保留。
- **奖励目录硬校验素材**：只要缺一张奖励图或语音，整个插件就无法加载；
  现在只记录缺失，渲染时再回退文字。
- **`/规则` 文案与实现不符**：规则写着“持有角色任意卡后可设为伙伴”，
  实际 17 名主角色都不需要持有卡。
- **`/角色语音 星咲 あかり 1` 无法解析**：含空格的原作名加序号会被判成参数格式错误。
- **卡册分页细节**：最后一行徽章与文字会溢出白色卡片；卡册图片渲染失败时没有任何回复。
- **版式调整**：好感心形的填充、数字与光晕对齐；卡牌揭示版式按机台参考照片调整。

更详细的复现过程与验证证据见本地维护记录（归档的底层检查与开发流水账，不随发布包分发）。

## 1.2.1 - 2026-09-10

修复随机任务难度与维护时间问题，并补齐任务历史清理能力。

- 挑战任务改为从真实曲库中随机选择歌曲，并选取该曲达到门槛的最低谱面，
  任务要求明确为“该谱面或以上，S 及以上”，不再把歌曲最高难度锁定为任务谱面；
  `challenge_min_level` 配置现在真正生效。
- 终极任务要求仅保留目标谱面与评级（如 `MASTER 15+ · SSS+ 评级`），不再重复显示
  “该谱面定数 15.5 ≥ 14.7”一类字段；任务卡超长曲名会按可用宽度截断并追加省略号。
- 插件每次加载和每日维护时，会按真实日期同步所有用户的周 5 连保底、囤点周期，
  并立即处理跨日任务；时间状态记录持久化，不再依赖插件连续运行时长。
- 修复初始化构建卡池时未读取持久化轮替起点的问题；重启后直接进入正确轮替位置，
  不会先回退到第 0 期再被命令同步。
- official 模式支持同一天多个官方卡池同时启用：`/卡池` 列出全部启用池，
  默认抽取最近开启的池，`/抽卡 <池ID> <数量>` 可指定其他池；
  `/天井列表` 也会列出全部启用池状态。
- official 模式改为按「几月几日」匹配历史官方池（忽略年份），用于无法获取最新
  排期时按同期历史排期模拟；跨年池按年末尾段 + 年初头段处理，结束日仍按不含当天
  计算。
- `/卡池` 在 official 多池模式下按池分别显示你的天井进度，满额时提示使用
  `/天井池 <池ID> <卡ID>`；卡池时间与抽卡结果会带上池 ID，避免多池同时启用时混淆。
- 修复指定未启用卡池抽卡时提示不准确的问题：现在会明确返回「指定卡池 X 当前未启用」
  并列出当前启用池，而不是笼统提示「当前没有可用卡池」。
- 全部指令回复统一渲染为分页图片卡片，渲染或发送失败时自动回退合并转发/纯文本；
  长文本按单页高度自动分页，并在标题与页脚标注页码。
- 统一卡片排版与文案：字段使用全角「｜」分隔、超长卡池名/卡名截断加省略号、
  `/卡池` 增加当天日期与 UP 卡总数，签到、天井兑换等提示语改为更清晰的短句。
- `/卡池 列表` 改为只输出 UP 角色信息图片，不再附带类型/时间/天井等详情与卡池公告图；
  `/卡池` 概览仍会发送各启用池的官方公告图，并修复 `/卡池 下一期` 重复显示类型/时间的问题。
- 修复天井指令的已兑换状态提示：`/天井 <卡ID>` 只列出仍可兑换的池，
  并附注已兑换池；全部兑换后明确提示已无可兑换池；`/天井池` 对未启用的池 ID
  改为提示「卡池 X 当前未启用」并列出当前启用池。
- 修复每日任务配额用尽时的提示混用内部英文标识：现在显示「今日普通/挑战任务
  次数已用完」。
- 新增 `/任务清理 [天数]` 管理员命令，可立即清理已审核、已过期、已重置的历史任务；
  待审核任务始终保留，审计日志不受影响。
- 新增 `auto_cleanup_history` 与 `task_history_retention_days` 配置，默认开启并保留
  30 天，避免任务列表与数据库长期膨胀。
- 修复签到彩蛋卡只显示文字、不展示卡面的问题：命中 `non_gacha_pool` 时改为渲染并
  发送单卡图，图片生成失败时自动回退文字提示。
- 规则与较长列表（卡池、卡册、任务、待审核等）改为排版成图片卡片发送，
  图片渲染或发送失败时回退为原来的合并转发消息；过期文本图与彩蛋卡图会随每日维护清理。
- 修复 `/规则` 末尾“【说明】”小节为空的问题，现在会列出 UTC 时间与配置调整说明。
- 更新 README、USAGE、规则文案和配置示例，明确挑战/终极任务的难度含义。

## 1.2.0 - 2026-09-08

新增音游随机任务系统，覆盖音击、舞萌 DX 与中二节奏。

- 接入三游戏真实曲库，统一归一化难度标识、标级、定数与曲绘地址。
- 新增普通任务、挑战任务与终极任务：普通任务任意难度；挑战任务锁定 10 级或以上
  谱面并允许“该难度或以上”；终极任务锁定定数 14.7 或以上谱面要求 SSS+。
- 接取任务时发送柔和渐变任务卡，包含任务 ID、曲目、谱面、要求、奖励和接取人。
- 普通/挑战任务按每日次数限制，未完成与已拒绝任务在每日 00:00 自动过期；已提交待审核任务保留。
- 管理员人工审核并发放点数：普通 20；挑战 S 30、SS 40、SSS/SSS+ 50；终极 30000。
- 终极任务完成后歌曲从该用户个人池永久移除，管理员确认后才能接取下一个。
- 新增隐藏管理员 `/任务重置 <任务ID>` 指令，用于处理无法游玩对应游戏等情况。
- 优化帮助文案，移除“本地娱乐插件”类字段，相关说明保留在文档中。
- 曲绘或任务卡图片生成失败时保留完整任务信息，并自动返回文字模式。
- 曲绘下载优先使用系统 `curl` 绕过服务端 WAF，失败时回退 Python 下载；同时保留 `!webp` 与 `assets2` 多 URL 兜底，并增加重试与本地缓存复用。
- 修复挑战任务接取时奖励显示为 0 的问题，默认按 S 档 30 点。
- 任务卡完整时只发送图片，不再重复返回文字；卡片不完整时才回退文字模式。
- 普通/挑战任务列表每日按 UTC 清理过期记录，已提交待审核任务保留给管理员。
- 日期切换改为国际时间 UTC；囤点档位奖励默认每 60 天重置，可在 `[economy]` 调整。
- 卡池轮替周期改为持久化起始日，插件重启不会重新计算 15 天周期。
- 修复抽卡落地失败时回滚半价/周保底状态可能抛 `NameError` 的问题，所有数据库时间戳统一为 UTC。
- 随机任务支持指定游戏：`/接任务 终极 中二`；不指定时默认三游戏全随机。

## 1.1.0 - 2026-09-05

新增官方卡池排表、卡内权重、池子轮替、连续签到、月卡和管理员发放点数功能，并统一整体产品名为“音击抽卡模拟器”。

- 修正历史卡池候选模型：活动池不再只包含公告列出的新卡/选择卡，而是按活动日期混入当时已有全部 R/SR/SSR，再叠加本期 UP 与天井选择卡；常驻池同步扩为当前版本全部基础卡。
- 抽卡结果图底部按 RinNET 样式叠加卡牌数字 ID 与完整卡号；`/卡图` 保持发送原始高清卡面。
- 获取 SEGA CARDMAKER 官方公告中的 63 个历史卡池，覆盖 2020-10～2026-07。
- 导入 Artemis `static_gachas.csv` / `static_gacha_cards.csv` 的卡内权重、UP 与选择标记。
- 支持 `cycle` 历史池轮替（默认每 15 天换池）和 `official` 官方日期选池两种模式。
- 抽卡改为“稀有度权重 → 池内卡权重 → UP 倍率”，UP 卡默认 ×10。
- 新增 `/卡池` 命令，可查看本期池、UP 数量、天井点数和轮替进度。
- `/抽卡` 和 `/概率` 现在显示当前卡池与 UP 信息。
- `/卡池` 会获取 63 期官方公告主图并随文字一起发送，图片本地缓存 7 天。
- 新增常驻池：包含当前版本已有的全部 R/SR/SSR 基础卡，可通过 `/抽卡 常驻` 单独抽取；`/抽卡` 默认仍抽取当前轮替活动池。
- 非抽卡来源卡从所有抽卡池中剔除，改为每日签到按概率随机获得；排表每条卡记录附带 `version` 和 `cardNumber` 用于复盘 ID。
- 落地天井机制：抽卡按池累计天井点，满点后可通过 `/天井 <卡ID>` 兑换当前池选择卡；每个池独立状态且只能兑换一次。
- 优化指令文案：精简 `/帮助`，新增 `/规则` 转发详细说明；长文本和可选卡列表自动使用转发消息发送。
- `/卡池` 增加当前池 UP SSR 角色与 ID 展示，`/卡池 列表` 可查看完整 UP SSR 列表。
- 明确无 UP 池的文案：`UP 卡：0 张` 只表示官方公告没有概率提升名单，抽卡候选仍为当期版本已有全部 R/SR/SSR；新增独立 `/天井列表` 入口，可查看当前池全部天井选择卡的 ID 与角色。
- `allow_local_operator` 默认关闭，管理员发放默认只允许 QQ 白名单。

- 连续签到每天递增奖励，最高每日额外 100 点；断签重新计算。
- 连续签到第 7 天、14 天、21 天等额外获得 500 点。
- 新增月卡：默认 1500 点购买 30 天，每日签到额外 100 点，剩余不超过 3 天可续费，购买后获得两次半价五连。
- 新增管理员配置和 `/奖励 <QQ号> <点数> [备注]`，并支持通过 `@用户` 直接指定目标，发放记录写入审计日志。
- 概率、卡牌信息、素材目录和新增配置支持 WebUI 热更新，不再需要重启插件。
- 重新平衡奖励：日常签到 100～250 点，新增 15 天卡池周期奖励 1000 点；新增 1000/3000/10000 点囤点档位奖励 100/500/2000 点，周奖励、月卡加成和半价次数同步调高，长期签到和囤点抽卡收益更明显。
- 清理开发期内部叫法，用户可见文案统一为“音击抽卡模拟器”。
- 新增 `CARD_ARTWORK_SOURCES.md`，说明卡面素材不随插件分发的原因、
  公开可查询的信息渠道、用户自行获取/校验素材的流程和版权注意点。
- `card_data_manifest.json` 现在同时登记 `gacha_pools.json` 的 SHA-256，
  `tools/sync_card_data.py --check` 可校验卡池排表完整性。
- `tools/sync_card_data.py` 默认素材来源改为插件自身的 `assets/card_data/`，
  缺少素材时输出明确提示，并引导阅读卡面获取说明。

## 1.0.0 - 2026-09-04

首个公开发布版本。

### 抽卡

- 支持 1 连、5 连、11 连抽取。
- 采用两阶段抽取：先按稀有度权重，再在对应稀有度内抽取具体卡牌。
- 5 连每用户每周首次提供 SR 或以上保底，周四 00:00（UTC）重置。
- 11 连始终提供 SR 或以上保底。
- 支持以 QQ 用户为单位的独立点数、签到、库存和抽卡日志。

### 收藏与展示

- 支持通过命令查看卡册进度。
- 支持 `/卡图 <ID>` 查看已拥有卡牌的高清原图。
- 抽卡结果图支持 1 连、5 连和 11 连构图。
- 展示星级、MAX 标记、解花和超解花状态。

### 数据与部署

- 插件默认读取 `assets/card_data/` 中的卡牌 JSON 和卡面素材。
- 配置文件允许手动切换卡牌信息与卡面目录。
- 提供 `tools/sync_card_data.py` 同步和 SHA-256 校验工具。
- 抽卡临时结果图保留 24 小时并自动清理。
- 每日签到包含当前配置的基础随机奖励。

### 文档与许可

- README 改为简短产品介绍。
- 新增使用说明和详细免责声明。
- 代码采用 AGPL-3.0-or-later；卡面素材权利归原权利人所有。
